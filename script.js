    // const timeEl = document.getElementById('time');
 
    // function updateTime() {
    //   timeEl.textContent = Date.now();
    //   timeEl.setAttribute('datetime', new Date().toISOString());
    // }
 
    // updateTime();
    // setInterval(updateTime, 500);


    /* ── Time CLOCK ─
       Date.now() called every 500ms and at load.
       textContent is the raw integer milliseconds —
       exactly what automated tests read via data-testid.
     */
    